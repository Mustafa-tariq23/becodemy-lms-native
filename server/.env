############################################################
# Server .env (NOT COMMITTED) - placeholders only here now
############################################################
PORT=5000
NODE_ENV=development

# MongoDB connection string (rotate credentials if leaked)
DATABASE_URL="mongodb+srv://<user>:<pass>@<cluster-host>/<db>?retryWrites=true&w=majority"

# JWT signing secret (rotate if leaked) - must be long & random
JWT_SECRET="<generate-random-64-char-hex>"

# (Optional) OAuth secrets - never expose to client
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=

# Firebase Admin service account (if needed) - store file path or JSON pieces
# FIREBASE_ADMIN_PROJECT_ID=
# FIREBASE_ADMIN_CLIENT_EMAIL=
# FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n<escaped-newlines>\n-----END PRIVATE KEY-----\n"

# Add any other private config below